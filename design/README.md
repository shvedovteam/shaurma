# Design source

Canonical editable design lives in Figma:

https://www.figma.com/design/X2Os31gFTJ8oVP3iToHT8b

Figma file key: `X2Os31gFTJ8oVP3iToHT8b`

## Approved frames

- Desktop / Home + Menu — node `1:2`, 1440 × 2700
- Mobile / Home + Menu — node `1:78`, 390 × 2420
- Desktop / Cart + Checkout — node `6:2`, 1440 × 1700
- Mobile / Cart + Checkout — node `6:90`, 390 × 2050

## Design status

The current visual direction is **approved**. Implementation should preserve the existing visual language and ordering UX rather than redesigning the product during development.

Food-photo areas remain placeholders until real or approved temporary imagery is selected.

## Important

The native `.fig` document cannot be exported or committed through the current Figma connector. The Figma URL above is therefore the canonical editable source. This repository stores the handoff specification, design tokens, references and implementation code.

When implementation starts, the UI should be built against the Figma nodes above rather than recreated from memory.
